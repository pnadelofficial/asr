# Python 3.11 ONLY.
# torch 2.1.2 publishes wheels for cp38-cp311 only; on 3.12+ pip fails with
# "No matching distribution found for torch==2.1.2".

# --- core DL stack -----------------------------------------------------
# The PyPI torch 2.1.2 wheel is the cu121 build and bundles its own CUDA
# libraries, so it runs fine on the CUDA 12.2 base image.
torch==2.1.2
torchaudio==2.1.2
triton==2.1.0            # openai-whisper requires triton>=2; 3.x targets torch>=2.4

# --- numeric -----------------------------------------------------------
numpy==1.26.4
scipy==1.17.1            # requires numpy>=1.26.4,<2.7 -- satisfied exactly at the floor
matplotlib==3.11.1       # requires numpy>=1.25
soundfile==0.14.0
tqdm==4.70.0

# --- ASR ---------------------------------------------------------------
openai-whisper==20250625
numba==0.61.2            # pulled in by whisper; needs numpy>=1.24,<2.3

# --- diarization -------------------------------------------------------
# pyannote.audio 3.2+ all require torchaudio>=2.2.0, so 3.1.1 is the newest
# release compatible with the torch 2.1.2 pin above.
pyannote.audio==3.1.1
# 3.1.1 declares only speechbrain>=0.5.14, but imports speechbrain.pretrained,
# which was renamed to speechbrain.inference in speechbrain 1.0. Without this
# pin the image builds and then crashes on the speaker-embedding model.
speechbrain==0.5.16

# If pyannote breaks on a lightning API change, pin lightning next:
# lightning==2.1.4